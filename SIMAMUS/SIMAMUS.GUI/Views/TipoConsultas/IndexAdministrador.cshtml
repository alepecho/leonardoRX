@model IEnumerable<SIMAMUS.GUI.Models.TipoConsulta>

@{
    ViewBag.Title = "IndexAdministrador";
    Layout = "~/Views/Shared/_LayoutAdministrador.cshtml";
}

<h2>IndexAdministrador</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreConsulta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreConsulta)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditAdministrador", new { id=item.IdTipoConsulta }) |
            @Html.ActionLink("Details", "DetailsAdministrador", new { id=item.IdTipoConsulta }) |
            @Html.ActionLink("Delete", "DeleteAdministrador", new { id=item.IdTipoConsulta })
        </td>
    </tr>
}

</table>
