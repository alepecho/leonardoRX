@model IEnumerable<SIMAMUS.GUI.Models.TipoConsulta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreConsulta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreConsulta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTipoConsulta }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTipoConsulta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTipoConsulta })
        </td>
    </tr>
}

</table>
