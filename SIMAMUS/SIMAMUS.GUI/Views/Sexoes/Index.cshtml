@model IEnumerable<SIMAMUS.GUI.Models.Sexo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Información general de los generos:</h2>

<p>
    @Html.ActionLink("Registrar nuevo género", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdSexo }) |
            @Html.ActionLink("Ver detalles", "Details", new { id=item.IdSexo }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdSexo })
        </td>
    </tr>
}

</table>
