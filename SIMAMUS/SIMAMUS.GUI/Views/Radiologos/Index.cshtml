@model IEnumerable<SIMAMUS.GUI.Models.Radiologo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Activo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.Contrasenna)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Activo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.Contrasenna)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodigoRadiologo }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodigoRadiologo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodigoRadiologo })
        </td>
    </tr>
}

</table>
